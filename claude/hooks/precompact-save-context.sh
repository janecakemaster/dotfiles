#!/bin/bash
# PreCompact hook: remind to save important context to memory before compaction
echo '{"systemMessage": "Context is about to be compacted. Save any important decisions, open threads, or new information to memory files before this happens."}'
